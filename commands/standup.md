---
description: Daily standup report — yesterday's commits and PRs, today's branch state
argument-hint: "[--all] [--since \"1 week ago\"] [--root <dir>]"
---

Prepare the daily standup report by following the `standup` skill.

Arguments to pass through to the collector script: $ARGUMENTS

If no arguments were given and the current directory is not a git repository, rerun the collector with `--all` rather than reporting failure.
